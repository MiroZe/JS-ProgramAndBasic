function easterShop (input){
    let index = 0;
let startQuantity = Number(input[index]);
index++;
let command = input[index];
index++;

while(command !== "Close"){
    
    switch(command){
        case "Buy":
        case "Fill" : 
    }

}



}